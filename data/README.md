# Local data

The proposed source is Kaggle's [Sales Conversion Optimization](https://www.kaggle.com/datasets/loveall/clicks-conversion-tracking) dataset.

Place the downloaded CSV at:

```text
data/raw/KAG_conversion_data.csv
```

The locally inspected file contains **1,143 data rows and 11 columns**. Its SHA-256 is:

```text
2ee88488b5229562e8814b08e95e09e675aa939f69fc16f124eefe2bfdfa7cf8
```

The original download/access date is unknown; the local file timestamp is not treated as proof of source access.

Obtain the CSV from the source and place it at the path above. Do not edit, reformat, or overwrite the original CSV. The `data/raw/` and `data/processed/` directories are excluded from Git, but this only prevents tracking—it does not delete local files.
